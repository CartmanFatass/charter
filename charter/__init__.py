"""EasyDMARC umbrella tooling.

A tiny, stdlib-only CLI that keeps an inventory of every repo in the
`easydmarc` GitLab group and clones them on demand into ``workspaces/``.

Run it as ``python3 -m edm`` (from the umbrella root) or via ``bin/edm``.
"""

__version__ = "0.1.0"
