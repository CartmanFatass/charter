# Tickets 01, 03, 04 merged (#62, #63, #65); 02 in review as #64. Correcti

_2026-08-12 14:41 · persistent_

Tickets 01, 03, 04 merged (#62, #63, #65); 02 in review as #64. Correction recorded: the grilling claimed fleet keeps a root tree — it does not, root_tree() returns None unless embedded, so a fleet plane never listed the plane root. The removal therefore closed the default-owns-root hole outright; what survives is that nothing PREVENTS working in the plane root, which is what 02/03 address.
