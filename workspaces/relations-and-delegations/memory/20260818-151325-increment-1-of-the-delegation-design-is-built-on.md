# Increment 1 of the delegation design is BUILT on branch persona-default-

_2026-08-18 15:13 · persistent_

Increment 1 of the delegation design is BUILT on branch persona-default-in-charter-toml (commits 0d9db69, f0f5d0e, 19a2a2d; 2430 tests green). What landed: charter.toml [persona] default (personas/.default still resolves one rung below, doctor names the migration); persona selection now per-session AND per-terminal like workspaces (.charter/sessions/<id>.persona, .charter/terminals/<id>.persona) with the plane-wide active-persona file kept only for a shell with no session and no pane id; a 'front door' doctor check plus one status-line row when the declaration names no persona; docs/personas.md front-door section; ADR 0016 'charter presents the roster; it never guesses the owner'. Two refactors fell out and both are one-source-of-truth fixes: persona.resolve_active/source now share _resolved (two ladders answering one question would drift the moment a rung was added), and workspace._terminal_id moved to session.terminal so personas do not relearn the WINDOWID lesson. One deliberate deviation from the settled design: the dangling default is doctor WARN, not FAIL — this repo reserves FAIL for 'you cannot work', and a plane with no persona still clones and still reaches its forge.
