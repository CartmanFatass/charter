# Increment 4 — 'require' level: tool-time ASK (never deny) on the first e

_2026-08-18 14:09 · persistent_

Increment 4 — 'require' level: tool-time ASK (never deny) on the first edit with no dispatch this turn. Sub-agent exclusion must be verified by running it, not reasoned about.
