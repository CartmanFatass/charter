# Increment 2 — per-persona 'routing: off|advise|require' + the roster sec

_2026-08-18 14:09 · persistent_

Increment 2 — per-persona 'routing: off|advise|require' + the roster section inside the EXISTING commitment-point gate (same trigger, same cooldown) + an advise/require event in the dispatch store + 'routing advice fired N · dispatched M' in persona stats.
