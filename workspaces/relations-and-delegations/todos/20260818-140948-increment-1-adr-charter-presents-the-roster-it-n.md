# Increment 1 — ADR 'charter presents the roster; it never guesses the own

_2026-08-18 14:09 · persistent_

Increment 1 — ADR 'charter presents the roster; it never guesses the owner' + [persona] default in charter.toml (personas/.default honoured, doctor names the migration) + per-session/per-terminal persona pointers + docs. The ADR ships FIRST: it is what stops increment 2 regrowing a keyword matcher.
