# Increment 3 — 'charter init --front-door <name>' (default steward) gener

_2026-08-18 14:09 · persistent_

Increment 3 — 'charter init --front-door <name>' (default steward) generating a GENERIC front-door persona that carries 'routing: advise' in its own frontmatter, and setting it as the plane default. The name lives in a flag, never in code.
