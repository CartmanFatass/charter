# Increment 5 — 'borrows:' splits 'uses:' per persona: declaring borrows t

_2026-08-18 14:09 · persistent_

Increment 5 — 'borrows:' splits 'uses:' per persona: declaring borrows takes tools from borrows and makes uses a routing edge for THAT persona only; declaring nothing keeps today's behaviour; 'borrows: none' borrows nothing.
